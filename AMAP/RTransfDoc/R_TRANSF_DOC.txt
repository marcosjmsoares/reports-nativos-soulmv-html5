--Repositório Completo: https://github.com/marcosjmsoares/reports-nativos-soulmv-html5
--Link do report: https://github.com/marcosjmsoares/reports-nativos-soulmv-html5/blob/main/AMAP/RTransfDoc/R_TRANSF_DOC.txt
--Nome módulo: AMAP - SISTEMA DE GERENCIAMENTO DE SAME

/*----------------------------------------------------------------------------*/
--R_TRANSF_DOC
SELECT  cad_same.cd_cad_same cd_cad_same
               ,cad_same.ds_cad_same ds_cad_same
               ,protocolos.cd_protocolo cd_protocolo
               ,to_char(protocolos.dt_saida, 'dd/mm/yyyy') dt_saida
               ,to_char(protocolos.hr_saida, 'hh24') hr_saida
               ,to_char(protocolos.dt_prev_retorno, 'dd/mm/yyyy') dt_prev_retorno
               ,protocolos.cd_portador cd_portador
               ,portadores.ds_portador ds_portador
               ,protocolos.ds_motivo ds_motivo
               ,same.nr_matricula_same nr_matricula_same
               ,it_same.cd_atendimento  cd_atendimento
               ,to_char(atendime.dt_atendimento,'dd/mm/yyyy') dt_atendimento
               ,to_char(it_same.dt_entrada,'dd/mm/yyyy')       dt_entrada
               ,sala_same.cd_sala_same cd_sala_same
               ,sala_same.ds_sala_same ds_sala_same
               ,same.cd_armario_same cd_armario_same
               ,armario_same.ds_armario_same ds_armario_same
               ,same.ds_linha ds_linha
               ,same.ds_coluna ds_coluna
               ,paciente.nm_paciente nm_paciente
FROM    dbamv.cad_same
       ,dbamv.same
       ,dbamv.it_same
       ,dbamv.it_same_protocolos
       ,dbamv.paciente
       ,dbamv.sala_same
       ,dbamv.armario_same
       ,dbamv.protocolos
       ,dbamv.portadores
       ,dbamv.atendime
WHERE cad_same.cd_cad_same = same.cd_cad_same
  and  atendime.Cd_Multi_Empresa = :CAMPO_CD_MULTI_EMPRESA
  and same.cd_paciente = paciente.cd_paciente
  and same.cd_cad_same = it_same.cd_cad_same
  and same.nr_matricula_same = it_same.nr_matricula_same
  and it_same.cd_atendimento = it_same_protocolos.cd_atendimento
  and same.cd_armario_same = armario_same.cd_armario_same
  and armario_same.cd_sala_same = sala_same.cd_sala_same
  and it_same_protocolos.cd_protocolo = protocolos.cd_protocolo
  and protocolos.cd_portador = portadores.cd_portador
  and it_same.cd_atendimento = atendime.cd_atendimento
  and protocolos.cd_protocolo = :CAMPO_CD_PROTOCOLO
  and atendime.cd_multi_empresa =  dbamv.Pkg_Mv2000.Le_Empresa
